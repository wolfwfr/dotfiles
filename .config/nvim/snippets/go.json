{
    "table_test": {
        "prefix": "tabletest",
        "body": [
            "    testcases := []struct {",
            "        desc string",
            "    }{",
            "        {},",
            "    }",
            "",
            "    for _, tc := range testcases {",
            "        t.Run(tc.desc, func(t *testing.T) {",
            "",
            "        })",
            "    }"
        ]
    }
}
